git -v
git init
git status
git add index.html
git commit -m "Message" index.html
git log (Check commit history)
git log --oneline
git show/git show commit id (Show commit info)
git shortlog (Shows commit message)
git diff commitid..commitid (To see the difference between id's)
rm rf * (It will delete in working tree)
git commit --amend -m "Message" (To change latest commit message)
git rebase -i HEAD~3 (To change the commit message)
git checkout master - index.html (To come back to latest version)
git rm index.html (To delete the file)
git checkout master - index.html (To restore the file)
git restore text.html (To restore to previous version before commit)
git restore --staged text.html (To unstage/untrack)
git restore filename (To restore deleted file)
git reset --soft HEAD^ (Uncommit and keep the changes)
git reset --hard HEAD^  (Uncommit and discard the changes)
git stash  (Park the file aside)
git stash apply (To get the stashed file back)
git push -u origin master
git clone  (Copy of remote repo to local repo)
